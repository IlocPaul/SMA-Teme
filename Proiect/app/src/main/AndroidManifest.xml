<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fitnessapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FitnessApp">
        <activity android:name=".MainMenu.ViewExercise"></activity>
        <activity android:name=".MainMenu.ListExercises" />
        <activity android:name=".MainMenu.MenuMainActivity" />
        <activity android:name=".Storage.ImagesActivity" />
        <activity android:name=".HomePage" />
        <activity android:name=".FragmentRecycler.FragmentRecyclerMainActivity" />
        <activity android:name=".Storage.StorageMainActivity" />
        <activity android:name=".RegisterUser" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>